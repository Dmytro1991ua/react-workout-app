import styled from 'styled-components';

export const SectionsHeading = styled.h1`
  font-size: clamp(1.5rem, -0.875rem + 9.5vw, 3.5rem);
  font-family: 'Akaya Telivigala', cursive;
  text-align: center;
  line-height: 1.5;
  text-transform: uppercase;
`;
